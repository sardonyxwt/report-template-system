export const KB = 1024;
export const MB = 1024 * KB;
export const GB = 1024 * MB;

export const MINUTE_SECONDS = 60;
export const HOUR_SECONDS = MINUTE_SECONDS * 60;
export const DAY_SECONDS = HOUR_SECONDS * 24;
export const SECOND = 1000;
export const MINUTE = MINUTE_SECONDS * SECOND;
export const HOUR = HOUR_SECONDS * SECOND;
export const DAY = DAY_SECONDS * SECOND;
export const WEEK = 7 * DAY_SECONDS * SECOND;

export const OAUTH_GOOGLE_KEY = 'oauth-google';
export const AUTH_COOKIE_KEY = 'jwt';
export const AUTH_REFRESH_COOKIE_KEY = 'jwt-refresh';

export const REQUEST_ID_HEADER = 'X-Request-Id';

export const ID_PATH_PARAM_NAME = 'id';
export const PDF_MIMETYPE = 'application/pdf';

export const TABLE_PAGE_SIZE = 20;
export const REFERENCE_ITEMS_LIMIT = 500;
